# CrystalBio staging migration rehearsal — 2026-06-21

Status: local staging rehearsal completed. No live app database, live app code path, or live photo storage was changed.

## Safety rules followed

- Used a copied JSON backup, not the actively written live file.
- Did not delete, hide, rename, overwrite, or discard live records.
- Did not print customer names, GPS values, passwords, tokens, or connection strings in this document.
- Kept embedded photo `dataUrl` bytes out of the PostgreSQL SQL file; photos were rehearsed as local private files and metadata references.
- Live backend remains on JSON persistence.

## Local staging setup

- Local Docker PostgreSQL staging container: `crystalbio-postgres-staging`.
- Schema applied from `db/postgres/schema.sql`.
- Migration SQL generated by `scripts/stage-crystalbio-postgres-migration.mjs`.
- Photo extraction rehearsal generated by `scripts/rehearse-crystalbio-photo-migration.mjs`.

## Count reconciliation result

Source copied backup and local PostgreSQL counts matched:

- agents: 17
- sessions: 48
- attendance records: 26
- sales records: 25
- sales visit updates: 24
- service records: 8
- service visit updates: 8
- leave requests: 0
- file attachments: 5

Photo rehearsal:

- embedded visit photo attachments found: 5
- local private rehearsal files written: 5
- photo metadata manifest created without embedded image bytes

## What this proves

- The current live JSON shape can be mapped into the draft PostgreSQL schema without losing row counts.
- `nextId`/sequence state is captured in migration metadata.
- Current embedded visit photos can be separated from app records into file objects with metadata references.
- This is still a staging proof, not production cutover.

## Remaining gates before live cutover

1. Choose and create the real managed PostgreSQL staging database.
2. Choose and create the private Cloudflare R2/S3 bucket.
3. Run the same migration against managed staging infrastructure.
4. Point a staging backend/app to PostgreSQL.
5. Bloom QA verifies login, Attendance, Sales, Service, leave, Field Entry, Reports/PDF, and photo view/upload.
6. Prepare rollback checklist and get explicit live-cutover approval.
